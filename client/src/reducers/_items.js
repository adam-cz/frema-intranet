const reducer = (_items = [], action) => {
  switch (action.type) {
    case '_ACTION1':
      return _items;
    case '_ACTION2':
      return _items;
    default:
      return _items;
  }
};

export default reducer;
